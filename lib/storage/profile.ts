// lib/storage/profile.ts
// Re-export shim — full implementation lives in profiles.ts (plural).
// Kept so any existing import path "@/lib/storage/profile" keeps working.
export * from "./profiles";
